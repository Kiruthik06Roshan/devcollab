import { PageFrame } from '@/components/common/PageFrame';

export default function NotificationsPage() {
  return (
    <PageFrame
      eyebrow="Inbox"
      title="Notifications"
      description="Real-time alerts for mentions, assignments, task movement, and project updates."
    />
  );
}