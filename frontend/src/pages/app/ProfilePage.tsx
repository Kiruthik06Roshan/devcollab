import { PageFrame } from '@/components/common/PageFrame';

export default function ProfilePage() {
  return (
    <PageFrame
      eyebrow="People"
      title="Profile"
      description="User identity, role, workspace memberships, and activity history will connect here."
    />
  );
}