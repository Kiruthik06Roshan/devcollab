import { PageFrame } from '@/components/common/PageFrame';

export default function SettingsPage() {
  return (
    <PageFrame
      eyebrow="Preferences"
      title="Settings"
      description="Appearance, notifications, security, and subscription settings will be consolidated here."
    />
  );
}