import { PageFrame } from '@/components/common/PageFrame';

export default function ProjectSnippetsPage() {
  return (
    <PageFrame
      eyebrow="Code"
      title="Snippet manager"
      description="Store reusable code snippets, notes, and quick references with project-scoped organization."
    />
  );
}