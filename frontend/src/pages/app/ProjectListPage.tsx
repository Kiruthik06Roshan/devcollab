import { PageFrame } from '@/components/common/PageFrame';

export default function ProjectListPage() {
  return (
    <PageFrame
      eyebrow="Tasks"
      title="List view"
      description="A compact table and filter view for task-heavy team workflows."
    />
  );
}