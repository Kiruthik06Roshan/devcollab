import { PageFrame } from '@/components/common/PageFrame';

export default function ProjectCalendarPage() {
  return (
    <PageFrame
      eyebrow="Planning"
      title="Calendar view"
      description="Sprint planning, due dates, and milestone timelines will be surfaced here."
    />
  );
}