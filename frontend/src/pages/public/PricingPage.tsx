import { PageFrame } from '@/components/common/PageFrame';

export default function PricingPage() {
  return (
    <PageFrame
      eyebrow="Plans"
      title="Pricing"
      description="Subscription tiers, limits, and billing flows will live here once the foundation is complete."
    />
  );
}